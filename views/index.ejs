<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Start Your Call</title>
    <!-- Tailwind CSS CDN -->
    <link rel="stylesheet" href="css/style.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
</head>
<body class="w-screen h-screen flex items-center justify-center bg-gradient-to-r from-blue-50 to-blue-100">
    <div id="centre-box" class="w-[40%] max-w-[400px] h-[40%] bg-white rounded-xl shadow-2xl flex flex-col items-center justify-center gap-8 px-6 py-8">
        <!-- Heading -->
        <h1 class="text-[28px] font-playfair font-bold text-gray-800 text-center">
            Start Your Call
        </h1>
        <!-- Button -->
        <a href="/call" 
            class="bg-cyan-600 text-white px-8 py-3 rounded-full text-[20px] font-inter font-semibold shadow-md hover:bg-cyan-700 hover:shadow-lg transition-all duration-300">
            Start Call
        </a>
    </div>
</body>
</html>

